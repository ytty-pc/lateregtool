- name: チェックスクリプト実行
        run: |
          SCRIPT=$(find $GITHUB_WORKSPACE -name "check.js" | head -1)
          HTML=$(find $GITHUB_WORKSPACE -name "index.html" | head -1)
          echo "Script: $SCRIPT"
          echo "HTML: $HTML"
          node "$SCRIPT" "$HTML"
